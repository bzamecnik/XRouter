<UserControl x:Class="XRouter.Gui.TokensViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <DataGrid Grid.Row="0" Name="uiTokens" MinHeight="300" ClipboardCopyMode="ExcludeHeader" SelectionMode="Single" SelectionUnit="Cell">
        </DataGrid>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <TextBlock VerticalAlignment="Center">Page:</TextBlock>
            <TextBlock Name="uiPageNumber" VerticalAlignment="Center" Margin="5 0 0 0">1</TextBlock>
            <Button Click="PreviousPage_Click" Margin="5 0 0 0">Previous</Button>
            <Button Click="NextPage_Click" Margin="5 0 0 0">Next</Button>
        </StackPanel>
    </Grid>
</UserControl>
