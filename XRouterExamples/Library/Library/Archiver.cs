using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Library
{
    class Archiver
    {
        // - look for recent changes (eg. yesterday's loans), generate a report
        // and store it to a file (send it to directory adapter of XRouter)
        // - create a catalog of all available books, store it to file
    }
}
